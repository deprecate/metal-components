{namespace Layout}

/**
* @param data
*/
{template .render}
	<div class="layout">
			{foreach $value in ($data)}
				<div class="row">
					{foreach $column in ($value.columns)}
						{call LayoutCol.render data="$column" /}
					{/foreach}
				</div>
			{/foreach}
	</div>
{/template}
