{namespace Layout}

/**
* @param rows
*/
{template .render}
	<div class="layout">
			{foreach $row in ($rows)}
				<div class="row">
					{foreach $column in ($row.columns)}
						{call LayoutCol.render data="$column" /}
					{/foreach}
				</div>
			{/foreach}
	</div>
{/template}
