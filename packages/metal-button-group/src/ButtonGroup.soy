{namespace Templates.ButtonGroup}

/**
 * This renders the main element.
 * @param? elementClasses
 * @param id
 */
{template .content}
	<div id="{$id}" class="buttonGroup component{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
