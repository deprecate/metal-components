{namespace Templates.Clipboard}

/**
 * This renders the main element.
 * @param? elementClasses
 * @param id
 */
{template .content}
	<div id="{$id}" class="clipboard component{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
